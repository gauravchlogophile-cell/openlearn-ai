-- Local development seed. Run after `supabase start` + migrations.
-- Creates demo auth users only in local stacks (uses supabase_auth helpers if present).
-- Sprint 1 keeps this minimal; role-seeded users arrive with the admin fn migration.
select 'seed placeholder — demo users added in Sprint 2 with auth admin API' as note;
